{
  "_comment": "Final test configuration for simulator-v2. Seeds [401-405] are strictly held out — never used in training (TRAIN_SEEDS=[101-104]) or validation (VALIDATION_SEEDS=[201-204]).",
  "simulator_version": "v2",
  "test_seeds": [401, 402, 403, 404, 405],
  "test_scenarios": [
    {
      "name": "canonical_cooling_failure",
      "description": "Canonical demo scenario. Zone-D (index 3) failure at step 50, 60% remaining capacity.",
      "scenario": "cooling_failure",
      "failure_injection": {
        "zone_index": 3,
        "trigger_step": 50,
        "reduced_capacity": 0.60
      }
    },
    {
      "name": "unseen_scenario_1",
      "description": "Unseen failure scenario. Zone-C (index 2) failure at step 40, 50% remaining capacity.",
      "scenario": "cooling_failure",
      "failure_injection": {
        "zone_index": 2,
        "trigger_step": 40,
        "reduced_capacity": 0.50
      }
    },
    {
      "name": "unseen_scenario_2",
      "description": "Unseen failure scenario. Zone-A (index 0) failure at step 70, 55% remaining capacity.",
      "scenario": "cooling_failure",
      "failure_injection": {
        "zone_index": 0,
        "trigger_step": 70,
        "reduced_capacity": 0.55
      }
    }
  ],
  "note": "Old seeds [301-305] are audit seeds for simulator-v1 only. Seeds [401-405] are the final test seeds for simulator-v2. These seeds must NEVER appear in engine/control/joint_rl.py, engine/control/cooling_only_rl.py, or any training/validation loop.",
  "audit_seeds_v1": [301, 302, 303, 304, 305],
  "historical_audit_seeds_contaminated": [42, 123, 456, 789, 1337]
}
